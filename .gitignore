/target
**/*.rs.bk
*.pdb
.*.swp
# Cargo.lock intentionally not committed for this library crate.
/Cargo.lock

# Claude Code session/worktree state
.claude/

# Claude Code agent memory (belongs in the scratch memory dir, not the repo)
/memory/
